
#ifndef MINIOS_MUTEX_H
#define MINIOS_MUTEX_H

/*
typedef struct {
    int locked;                // 0 = libre, 1 = occupé
    int owner_pid;            // PID du processus propriétaire
    queue_t wait_queue;       // File des processus en attente
} mutex_t;
*/

#endif //MINIOS_MUTEX_H
